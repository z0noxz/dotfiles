#!/bin/sh

set -- $(ls ~/.password-store/backup/borg/nas); for repo
do
    export BORG_REPO="backup:$repo"
    export BORG_PASSCOMMAND="pass backup/borg/nas/$repo/passphrase"

    echo "status: $repo"
    /bin/borg list
    echo
done
