#!/bin/sh

args=" -p layouts: -l 20"
options="grid\ncolumns\ntiled\nfloating\nmonocle\ntcl"

layout="$(printf "$options" | dmenu_vtc${args})"
case "$layout" in
    grid|columns|tiled|floating|monocle|tcl)
        echo "set layout $layout" > "/tmp/dwm.fifo";;
esac
